@extends('layouts.app')
@section('content')
<div class="rounded bg-white p-5 shadow"><div class="flex justify-between"><h1 class="text-2xl font-bold">{{ $title->title }}</h1><x-status :value="$title->status" /></div><dl class="mt-4 grid gap-3 md:grid-cols-2"><div><dt class="text-sm text-slate-500">Mahasiswa</dt><dd>{{ $title->student->name }}</dd></div><div><dt class="text-sm text-slate-500">Dosen Pembimbing</dt><dd>{{ $title->supervisor->name ?? '-' }}</dd></div><div><dt class="text-sm text-slate-500">Tanggal Persetujuan</dt><dd>{{ optional($title->approved_at)->format('d/m/Y H:i') ?? '-' }}</dd></div><div><dt class="text-sm text-slate-500">Catatan</dt><dd>{{ $title->notes ?? '-' }}</dd></div></dl><div class="mt-5"><h2 class="font-bold">Latar Belakang</h2><p class="whitespace-pre-line">{{ $title->background ?: '-' }}</p>@if($title->status === 'disetujui')<div class="mt-5"><a target="_blank" href="{{ route('titles.approvalLetter',$title) }}" class="rounded bg-green-600 px-4 py-2 text-white">Download Surat Penunjukan Pembimbing</a></div>@endif</div>
@endsection
