@extends('layouts.app')
@section('content')
<div class="mb-4 flex items-center justify-between"><h1 class="text-2xl font-bold">Pengajuan Judul</h1>@if(auth()->user()->isMahasiswa())<a href="{{ route('titles.create') }}" class="rounded bg-indigo-600 px-4 py-2 text-white">Ajukan Judul</a>@endif</div>
<form class="mb-4 grid gap-2 md:grid-cols-4"><input name="q" value="{{ request('q') }}" class="rounded border p-2 md:col-span-2" placeholder="Cari judul/mahasiswa"><select name="status" class="rounded border p-2"><option value="">Semua status</option>@foreach(['diajukan','disetujui','ditolak','revisi'] as $s)<option value="{{ $s }}" @selected(request('status')==$s)>{{ $s }}</option>@endforeach</select><button class="rounded bg-slate-800 px-4 py-2 text-white">Filter</button></form>
<div class="overflow-x-auto rounded bg-white shadow"><table class="w-full text-sm"><thead class="bg-slate-100"><tr><th class="p-3 text-left">Judul</th><th>Mahasiswa</th><th>Dosen</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $t)<tr class="border-t"><td class="p-3 font-medium">{{ $t->title }}</td><td>{{ $t->student->name }}</td><td>{{ $t->supervisor->name ?? '-' }}</td><td><x-status :value="$t->status" /></td><td class="space-x-2"><a class="text-indigo-700" href="{{ route('titles.show',$t) }}">Detail</a>@if(auth()->user()->isJurusan() || (auth()->id()==$t->student_id && in_array($t->status,['diajukan','revisi'])))<a class="text-blue-700" href="{{ route('titles.edit',$t) }}">Edit/Review</a>@endif</td></tr>@empty<tr><td colspan="5" class="p-4 text-center text-slate-500">Belum ada data.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $items->links() }}</div>
@endsection
