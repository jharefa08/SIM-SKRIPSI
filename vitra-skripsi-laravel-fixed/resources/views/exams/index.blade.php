@extends('layouts.app')
@section('content')
<div class="mb-4 flex items-center justify-between"><h1 class="text-2xl font-bold">Pendaftaran Seminar/Sidang</h1>@if(auth()->user()->isMahasiswa())<a href="{{ route('exams.create') }}" class="rounded bg-indigo-600 px-4 py-2 text-white">Daftar Sidang</a>@endif</div>
<form class="mb-4 grid gap-2 md:grid-cols-4"><select name="type" class="rounded border p-2"><option value="">Semua jenis</option>@foreach(['seminar_proposal','sidang_skripsi'] as $s)<option value="{{ $s }}" @selected(request('type')==$s)>{{ str_replace('_',' ',$s) }}</option>@endforeach</select><select name="status" class="rounded border p-2"><option value="">Semua status</option>@foreach(['diajukan','diverifikasi','dijadwalkan','ditolak','selesai'] as $s)<option value="{{ $s }}" @selected(request('status')==$s)>{{ $s }}</option>@endforeach</select><button class="rounded bg-slate-800 px-4 py-2 text-white">Filter</button></form>
<div class="overflow-x-auto rounded bg-white shadow"><table class="w-full text-sm"><thead class="bg-slate-100"><tr><th class="p-3 text-left">Jenis</th><th>Mahasiswa</th><th>Jadwal</th><th>Ruangan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($items as $e)<tr class="border-t"><td class="p-3">{{ str_replace('_',' ', $e->type) }}</td><td>{{ $e->student->name }}</td><td>{{ $e->scheduled_at?->format('d/m/Y H:i') ?? '-' }}</td><td>{{ $e->room ?? '-' }}</td><td><x-status :value="$e->status" /></td><td class="space-x-2"><a class="text-indigo-700" href="{{ route('exams.show',$e) }}">Detail</a>@if(auth()->user()->isJurusan())<a class="text-blue-700" href="{{ route('exams.edit',$e) }}">Verifikasi/Jadwal</a>@endif</td></tr>@empty<tr><td colspan="6" class="p-4 text-center text-slate-500">Belum ada data.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $items->links() }}</div>
@endsection
